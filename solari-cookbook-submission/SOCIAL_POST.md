# Social posts (draft)

Not a job application — a Cursor multi-model showcase for the Solari Cookbook challenge.

Public artifact (this environment cannot fork `solari-sdk/solari-cookbook`):
https://github.com/tygart-media/notion-data-source-compat/pull/2

Example directory:
https://github.com/tygart-media/notion-data-source-compat/tree/cursor/a11y-render-check-rewrite-dbe4/solari-cookbook-submission/examples/accessibility-render-check-ts

## X (≤280)

```
Not a job application — a Cursor multi-model showcase for @getsolari.

Opus + Grok + Gemini + Fable in 1 session: tore down a broken draft & shipped an a11y checker proving served HTML can pass CI while Solari's cloud browser catches post-paint defects.

https://github.com/tygart-media/notion-data-source-compat/pull/2
cc @harrychow_
```

## LinkedIn

```
Not a job application — Cursor multi-model showcase for the Solari Cookbook challenge.

In one Cursor Cloud Agent session we coordinated four frontier models:

• Grok — orchestration, competitor PR teardown, demo narrative
• Gemini — cookbook voice / PMF
• Fable — SDK/API correctness (@solarisdk/browser + @solarisdk/sdk)
• Opus — staff-engineer rewrite; rejected a circular first draft that failed every run

Example: accessibility-render-check-ts
Static a11y linters only see served HTML. Visitors get the DOM after theme/widget scripts. Stage 2-source-fix is the point: served=0 defects, rendered=1.

Public writeup + code:
https://github.com/tygart-media/notion-data-source-compat/pull/2

Upstream handoff commands (fork blocked in this agent token): see UPSTREAM.md in the PR.

@harrychow_ @getsolari
```

## Still needed before posting

1. Live `SOLARI_API_KEY` run — replace README sample with a real transcript
2. Human with fork rights runs UPSTREAM.md → PR into solari-sdk/solari-cookbook
3. Then post
